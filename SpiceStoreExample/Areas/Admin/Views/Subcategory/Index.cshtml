@model IEnumerable<Subcategory>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<br />
<div class="border backgroundWhite">
    <div class="row">
        <div class="col-6">
            <h2 class="text-info"> Subcategory List </h2>
        </div>
        <div class="col-6 text-right">
            <partial name="_CreateButtonPartial" />
        </div>
    </div>
    <br />
    <div>
        @if (Model.Any())
        {
            <table class="table table-striped border">
                <tr class="table-secondary">
                    <th>
                        @Html.DisplayNameFor(m => m.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.Category.Name)
                    </th>
                    <th></th>
                    <th></th>
                </tr>
                @foreach (var subcat in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(m => subcat.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(m => subcat.Category.Name)
                    </td>
                    <td>
                        <partial name="_TableButtonPartial" model="subcat.Key" />
                    </td>
                </tr>
                }
            </table>
        }
        else
        {
            <p>No subcategory exists...</p>
        }
    </div>
</div>  